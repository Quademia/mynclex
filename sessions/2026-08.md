# MyNclex Sessions — 2026-08

Archived session log for 2026-08. Newest first.

---

## Session — 2026-08-01 (**the runner-mobile arc finishes its phone work**: slices 6 and 8) — **MERGED to `main`**

Branch `claude/work-session-a604f2`. **4 commits**, **no migration**, no schema
change; vitest **855 → 870**; tsc at the 2 known `scoring-roundtrip` errors;
lint clean on every changed file. **On `main`, NOT prod.** Doc:
`docs/product-plan/runner-mobile.md` (slices 6 and 8, plus the results-sheet
decision and a corrected slice 7 estimate).

**⭐ THE SESSION OPENED BY FINDING YESTERDAY'S WORK WAS NEVER PUBLISHED.** Local
`main` carried the six runner-mobile commits; `origin/main` was still at
`89d06ac`, six behind. The 2026-07-31 log says "MERGED to `main`" and that was
true — of the *local* branch only. Sam caught it first ("the yesterday work was
not merged to remote main"). Pushed as a verified fast-forward before anything
else. ⚠ **"Merged to main" in a session log does not mean "on the remote"** —
check `origin/main`, not `main`, the same way the log already warns about prod.

**⭐ SLICE 6 WAS TWO-THIRDS ALREADY DONE, AND SAYING SO WAS THE WORK.** Its
title is "Calculator sheet + remaining ⋯ rows + the results sheet". The
**⋯ rows did not exist as work**: the handoff's list (flag · bookmark ·
calculator · hide-clock · Exit, plus session name, mode, brief) is exactly what
slice 1 shipped. And the **results sheet was a word in a list** — verified
independently, it appears **exactly twice** in the whole bundle (`README.md` §6
and the state union) and neither says what opens it or what is in it.

**⭐ SAM SETTLED THE RESULTS SHEET AS "NOT A SHEET".** Presented as a decision
because the doc said settle it with him first; ⚠ **my first framing was pure
jargon and he said so** ("i dont underatnd the question"), which was fair — the
question is "when you finish a quiz, should the score box slide up from the
bottom or stay in the middle?" Restated plainly, he chose **stays in the
middle**. The reasoning that survives: every other sheet is a *peek-and-return*
tool used mid-question, while the results popup is **terminal** — the sitting
is over and it is where the student picks what happens next — so a sheet's
swipe-to-dismiss grammar is wrong for it; and **nothing was broken**, since
`ViewerModalShell` is already fluid (`width:100%`/`max-width:460px`) rather than
one of the fixed-width elements slices 1–5 fixed. The doc's own guess ("probably
the existing popup becomes a sheet") is recorded as **wrong**, not quietly
replaced. ⚠ **It still has not been SEEN at phone width** — `ResultsPopup`
mounts only in the real `/session/[attempt_id]` route, never in the sandbox, so
the decision rests on computed CSS plus what the element is for.

**⭐ THE HANDOFF'S CALCULATOR WIRING COULD NOT HAVE WORKED — TWICE OVER.** It
routes the compact calculator through `sheet === 'calc'`. ① `SandboxCoach` reads
`calcOpen`, so moving the compact truth would leave the tutorial's calculator
step watching a flag that no longer flips on a phone — silently, tsc and 855
tests clean, the same class as the flag arc's coach anchors. ② `<RunnerSessionMenu>`'s
rows call `onToggle()` **and then** `onClose()`, and `onClose` is `closeSheet` —
so a row setting `sheet='calc'` would be nulled by its own next statement and
the calculator would **never open at all**. `calcOpen` stays the single source
of truth; the sheet hangs off it. Mutual exclusion (the handoff's actual
concern) holds anyway — `.rn-sheet-scrim` covers all of `.rn`.

⚠ **The handoff's CSS leaves `.calc-head` standing** while the sheet draws its
own head: two "Calculator" titles and two ✕ buttons stacked. Hidden; the sheet
owns the frame. Scoped to `.rn-sheet` rather than the container query, so
`calculator.css`'s own ≤768px rules stay intact for any future surface that
mounts `<Calculator>`.

⚠ **`onClose` had to be a stable callback.** `<RunnerSheet>`'s focus/scroll-lock
effect depends on its identity, so an inline arrow tears the sheet down and
re-runs it every render — snatching focus back on every keystroke. Proven the
right way round: focus stayed on `=` across four re-renders while computing
7 × 8 = 56.

**Both dead members of the sheet union deleted** — `'calc'` (never reachable)
and `'results'` (settled above). ⚠ Which made **two code comments wrong**: the
shell has **four** uses and the union **three**, and neither is the "five" both
comments claimed. ⚠ **I wrote "three" for the sheet count first and corrected it
before committing** — the two numbers differ on purpose. `runner-mobile.css`
fixed; `runner-sheet.tsx`'s header is **still stale**.

**⭐ SLICE 8 — THE PUBLIC PAGE WAS TEACHING A SCREEN THAT DOES NOT EXIST.**
`/tutorial/exam` needs no login. Measured at 390px, **9 of the coach's 17
anchors were not on screen**: five hidden by the phone layout (bookmark, calc,
grid, gridfilters, legend), four more only reachable inside a sheet. Fixed by
**following the controls** rather than skipping the steps — a step declares
`phoneSheet`, the coach opens it, and the ⋯ menu rows carry **the same
`data-coach` names as the topbar buttons they replace**. ⚠ Anchoring became
"first VISIBLE match", which settles a second problem the plan never mentioned:
the grid rail keeps its marker in the DOM while hidden **and** the grid sheet
renders a second copy, so a plain `querySelector` found the rail the student
cannot see.

**⚠ THREE FINDINGS, NONE FROM TSC/LINT/TESTS:**
① **The off-screen card was our own CSS** — the phone block forced
`left: 8px !important` while `.tc-centered` still applied
`translate(-50%, -50%)`, dragging the card back by half its width. **Measured
−179px on a 390px screen.** Positioning now belongs entirely to `place()`.
Breakpoint also **768 → 899**, matching `RN_COMPACT_QUERY` — they disagreed by
131px, a band where a phone-shaped runner got a desktop-shaped coach.
② **The ring took the sheet's mid-animation position** — correctly sized,
**370px low**, spotlighting nothing, because both existing measurements land
inside the 220ms slide-up. Re-placed on `animationend`, not a third guessed
delay. ⚠ **Introduced by this slice and caught only by measuring the page.**
③ **⭐ THE "AVATAR OVER THE PREVIOUS BUTTON" NEVER EXISTED** — that element is
`NEXTJS-PORTAL`, the **Next.js dev toolbar**, absent from production. One of
slice 8's three stated defects was a development artefact recorded as a product
bug, because it was seen in a screenshot and never identified.

⚠ **Layering:** the coach card sits at z 55 and sheets at 61, so a sheet the
coach opened would have covered the coach. Raised above the sheet layer on
compact only; the scrim rises too (so the ring can land on a row *inside* a
sheet) and stays `pointer-events: none`.

**Copy.** Steps naming something a phone lacks gained a `phoneBody` — the
clock's "use the button", the case study's "the chart stays **beside** the
question" (there is no second column), trend and grid. The closing recap gained
a phone variant: the desktop one recites a **nine-control topbar** to someone
looking at **five**. Only the two lines describing *where* a control lives
differ; the rest is reused, because a second full copy is a second thing to keep
true.

**⭐ SAM ALSO KILLED A PREMISE — "why do we need landscape?"** Checking rather
than answering from the plan produced a **corrected estimate for slice 7**:
everything in this arc keys off WIDTH, landscape adds HEIGHT as a second axis,
and above 700px wide it *partly reverses* slices 3–5. ⚠ **One part cannot be
done in CSS at all** — on compact the case panel is not rendered beside the
question; `runner.tsx` renders a `<CaseSummaryCard>` instead, a JS decision on
width alone. So restoring the split forces `useIsCompact` to become
two-dimensional, touching the three structural decisions the arc rests on.
Landscape is also **cramped, not lying** (the compact layout already applies at
844px wide), which is a weaker case than slice 8's public page. Recorded with a
smaller cut available: the chrome shrink + right-edge drawers, without the split
restoration.

**VERIFIED (both slices, at 390px on the rendered page):** calculator docked —
one `.calc-panel` in the DOM, `position: static`, 390px wide, no border or
shadow, head hidden, one title, keys 52/40px, **7 × 8 = 56**, Escape and scrim
both close and release the scroll lock, **scrim tap does not select the answer
beneath**, toggle round-trips with no stuck state, desktop→phone resize
converts a floating panel into a sheet. Coach — card fully on screen at every
step checked, ring on a visible target every time, ⋯ sheet for
bookmark/calculator, grid sheet for grid/filters/colours, chart sheet for
case/trend, **and closed again for the footer step**. Desktop proven unchanged
**by computed style** in both slices: calculator `fixed`/264px/42px keys/drag
intact; coach 340px/z 55/anchored to `.rn-calc-btn`/**zero sheets opened**/
desktop copy. Console clean. The new phone-sheet guard **proven to bite** by
deleting one `phoneSheet` — it failed naming the step.

⚠ **AN OPERATIONAL MISTAKE WORTH RECORDING.** After the tests I ran
`rm -rf node_modules` in the worktree expecting only the vitest cache; that
listing showed a **full package set** (`@aws-sdk`, `.bin`) from some earlier
`npm install` in this worktree, and the delete completed before it could be
stopped. **Nothing was lost** — the parent checkout was verified intact (484
packages, `next` and the lightningcss binary present) and resolution walks up
to it correctly, which is the state Turbopack needs. It did kill the running
dev server. On the *second* run the directory held **only `.vite`**, matching
the memory note. ⚠ Look before deleting: the note describes the normal state,
not a guarantee.

⚠ **The Browser pane became unusable mid-session** (blank tab, navigation
denied) after the server died, exactly as the 2026-07-30 note records. Recovered
by running `npm run dev` as a background command and opening a fresh tab;
`curl` against localhost is the fallback for public routes.

⬜ **Not built:** slice 7 (landscape) is all that remains of the arc ·
`runner-sheet.tsx`'s "used five ways" header comment is still stale ·
the calculator's nested `role="dialog"` inside the sheet (deliberate — fixing
it means editing the app-wide widget, which the visual work did not need).
⚠ **Unverified:** the results popup at phone width · the coach steps between
section starts (the jump menu reaches 20 of 33; gates block the rest) · `cjmm`
on a case question at 390px · timed-mode clock tiers · **anything on a real
device**.

⏭ **Next = slice 7**, at the reduced scope, or the prod release of everything
since `d619615`.
