# MyNclex Sessions Log

Index of MyNclex work sessions. Detail lives in period files (monthly by
default) under `sessions/`; this file is the index. When logging a new
session, decide whether to append to the current period file or start a
new one. Newest first. Product-local — per the extraction rule in
CLAUDE.md.

---

## Session files (newest first)

- [2026-06 — **2026-06-12 (third):** **Programme sidebar identity + Delivery section · cohort workspace folds into the Cohorts tab** (on session branch, Sam testing — not yet merged): two slices, all app-layer, no migration. **(1) Sidebar** (`d0646d7`): mode-specific tabs sit LAST under a labelled **"Delivery"** divider (new `NavItem.section`) so common tabs hold identical positions across both delivery modes; header gains the **programme name** (2-line clamp) + **Tutor-led / Self-paced chip**. **(2) The fold** (analysed → Sam pointed at the **library pattern** → planned in cohort-workspace-fold.md → built same day, `48af9f1` · `989c075` · `ff40707`): the cohort run detail renders **in place on the programme Cohorts tab** — `?cohort=` selects the run, `?tab=` picks Overview / Curriculum / Analytics / Sessions (placeholder kept) / Settings; pane bodies moved not rebuilt; only the active tab's data fetched. Old `/tutor/cohort/[id]` world (routes + shell + sidebar + back-pill + `TUTOR_COHORT_NAV`) **deleted**; one-file `[[...rest]]` redirect shim keeps old URLs alive; tutor Home + cards + revalidates rewired. *Cohort stops being a place and becomes a context* — no sidebar swap anywhere in the tutor app. Gotcha: PowerShell `Remove-Item` needs `-LiteralPath` for `[bracket]` folders.](sessions/2026-06.md)
- [2026-06 — **2026-06-12 (later):** **Enrolments move to programme level + zoom + width fix + convert-with-plan** (MERGED to `main`, not yet prod): executed the queued plan — the enrolment roster now lives at PROGRAMME level for BOTH delivery modes (programme = people & money, cohort = delivery). Tutor-led joins the programme Enrolments page (roster across all cohorts w/ **cohort tags**, the **Waitlist tab moved up** cohort-badged, add-student **cohort picker**, mode-driven summary cells); the cohort Enrolments entry + route **deleted**, cohort Overview gets a pre-filtered **"Manage enrolments →"** link; `RosterScope` retired (actions take `programmeId`; convert/dismiss read the lead row). Then 5 tested-and-agreed extensions: **cohort ZOOM** (filter recomputes cards + chips + waitlist page-wide, scope line + Show-all exit — the deep link restores the old cohort-scoped view) · **table width fix** (1400px cap · `overflow-x` + sticky Actions · row **⋯ menu**; menu **body-portaled** — sticky cells are stacking contexts) · **convert-with-plan parity** (Convert dialog = the Add-Student plan picker via shared `PlanPickerFields`; one dialog, one confirmation; lead stays PENDING on failure) · header **Students → Enrolments**. 6 commits `a1f454c`…`474239d`, all app-layer, **no migration**. ⚠ Process: build started without the session-start go-ahead — called out + memorised (discussion ≠ authorization, twice).](sessions/2026-06.md)
- [2026-06 — **2026-06-12:** **Self-paced Enrolments tab (programme level) + access-window freeze on tutor-add** (MERGED to `main`): an MVP-sweep pass over onboarding/payments/enrolment that surfaced the sweep's biggest gap — **self-paced enrolments had no tutor surface** (roster was cohort-only, programme Students tab a placeholder, no cohortless manual-add → off-platform self-paced was a dead end). Settled "the roster lives where students enrol" (cohort enrolments untouched; self-paced gets a programme-level **Enrolments** tab, mode-filtered in the sidebar). `EnrolmentRosterView` generalised behind a `RosterScope` union — programme scope is roster-only (Waitlist/PENDING_APPROVAL are cohort concepts; cells = Enrolled/Paused/Overdue/Expired); new `addSelfPacedStudentAction` creates `cohort_id = NULL` rows. **Behaviour fix:** ALL tutor-add paths (cohort add, waitlist convert, self-paced add) now freeze `access_expires_at` from the programme's access window like the paid path — the old always-lifetime was a Slice-1b leftover. All app-layer, no migration; one commit `0e31735`. Also: full onboarding/payments review (gaps: no transactional email, trial unwired, readiness packs, tutor sub, ⚠ `PAYSTACK_SECRET_KEY` missing on prod Worker) + confirmed the 06-09 release shipped (BUILD_LIST stale banner fixed). **Queued next:** "add student WITH a payment plan" (tutor-added ≠ off-platform money).](sessions/2026-06.md)
- [2026-06 — **2026-06-09:** **Programme Library tab (tutor "student preview")** (MERGED to `main`, not yet prod): a new **Library tab** on the tutor's programme detail (parallel to Quizzes) — a read-only **"student preview"** of the notes students in THIS programme can see. From the CD "Programme Library (tutor preview)" handoff; reuses the student library's `.lib-*`/`.lens-*` system. **All app-layer, no migration.** One commit (`61a50b7`), two slices. **Key finding:** the student read view's gating is RLS-visibility-based, which a **tutor satisfies by ownership** — so `RenderBlocks` + images + PDFs work unchanged (only the stateful pieces needed neutralising). **Slice 1 (list):** `getProgrammeLibrarySnapshot` = the tutor-side mirror of `nclex_student_can_see_note()` keyed on the programme (`TUTOR_WIDE ∪ PROGRAMME_SCOPED-to-here`, published; scoped by the programme's `tutor_id` so a SUPER_ADMIN preview stays correct) + `getProgrammeStudentCount`; a faithful mirror of the student shell + 3 tutor recontextualisations — **preview banner**, per-note **visibility chip** (All students / This programme), and a **Preview Home** (content counts, not per-student progress; Recent/Bookmarked dropped); new tutor scope parser; `library` entry in `TUTOR_PROGRAMME_NAV` after Curriculum. **Slice 2 (read view):** `getProgrammeNoteForRead` (ownership + explicit programme-entitlement gate, no per-student state) + a read-view mirror with **inert** bookmark/done (no writes) + **answerable-but-unsaved embedded questions** (`embed-preview.tsx` + `embed-grade-action.ts` — paged one-at-a-time Prev/Next; tutor can Check answer → feedback + rationale via a **grade-only** server action that scores but **writes no row**; "preview, not saved" note; no key reaches the client); one **additive** `ReadCtx.renderEmbed?` seam on the shared `read-blocks.tsx` leaves the student path unchanged. tsc + eslint clean; routes serve 200 on dev. Deferred (noted): visibility chip in read header, "By unit" placeholder (11.11), per-cohort visibility.](sessions/2026-06.md)
- [2026-06 — **2026-06-09:** **Quiz tags (author→store→show→find) + programme quiz list "grouped rows" redesign** (all MERGED to `main`, not yet prod): the whole tutor-quiz **tags** arc end-to-end, then the programme **Quizzes** tab CD redesign. **One migration** (`20260701120000_quiz_tags`, ships to prod next release); everything else app-layer. **Tags parts 1+2** (`97de6ce`) — `tags TEXT[]` on `nclex_tutor_quizzes` + a chip `QuizTagInput` in the shared create/edit `QuizFormModal` (server-normalised ≤40 chars/≤16; carried on `QuizListRow` so the card-pencil edit round-trips). **Card badges row** (`4ebfd17`, CD "badges row" Layout B) — Tags/Programmes/Activities chips each with a hover/click-pinned **peek** listing the real items; editor link wraps body+meta only, badges row outside it; `getMyQuizzes` now fetches programme **names** + a list-wide activities scan; `used_in_programmes` kept so the tutor Home is untouched. **Editor header badges** (`4a30c5b`) — same three, peeks open **downward** (`direction` prop), duplicate zone badge removed, `getQuizProgrammes` added. **Tags filter** (`e343fdf`) — multi-select `QuizTagFilter` (OR semantics, client-side) on the toolbar + tags folded into search; tagged 9 dev quizzes for testing. **Programme quiz list — CD Option B (grouped rows)** (`00a5ad9`) — two groups (**From the curriculum** vs **Standalone**) + a recontextualised badge cluster (Activities-here / Tags / **Other-programmes** reuse context); `getProgrammeQuizzes` returns tags + the full in-programme activity list + other-programme names; retired `source-hint.tsx`; new `ProgrammeQuizBadges` + `.prow*`/`.pgroup*` CSS. 3 reviewed rough edges left as-is per Sam. **Next session noted** (`ce71601`) — a programme-level **Library** tab (tutor side) mirroring the student programme library; key finding: the student library is **tutor-keyed** (no `COHORT_SCOPED`), so the programme is the right home. Captured in `tutor-library.md` + `BUILD_LIST.md`.](sessions/2026-06.md)
- [2026-06 — **2026-06-08:** **Curriculum workspace — CD-diff close-out** (merged to `main`, not yet prod): finished the master-detail curriculum rework (Stages 1+2 were already on `main`) by diffing against the actual CD "Tutor program" handoff and closing every gap + deferred item. **All app-layer, no migration.** **Sticky unit header / independent body scroll** (`.cur-detail-head` pinned, `.cur-detail-body` scrolls, full-width scrollbar at pane edge, new `.cur-detail-inner`). **Unpublish-unit confirm** — guards the rail-kebab + detail-header Unpublish, but ONLY when the programme `status==='PUBLISHED'` (advisory, reversible; unit-only; threaded programme status into `getUnitDetail` + workspace layout; self-portaling `UnpublishUnitConfirm`). **Add-unit length-change confirm** (symmetric to the length-decrease; `AddUnitConfirm`). **Unit reorder** (rail move up/down; `reorderUnitAction` 3-step temp-index swap for the UNIQUE(programme_id,unit_index) constraint). **Unit delete** — rail kebab + a quiet-danger button in the detail header; `deleteUnitAction` deletes + renumbers the tail DOWN then drops `length_units` (renumber-first → reconcile trigger no-ops, so NO model flip / migration needed); type-`DELETE` confirm for content units, plain for empty; blocks deleting the last unit. The feared units↔length "model flip" proved unnecessary. 6 commits `8dc0ef5` · `d1d5341` · `88dec82` · `231c7f4` · `73766dc` · `8b8242d`. Curriculum-workspace CD arc now fully closed.](sessions/2026-06.md)
- [2026-06 — **2026-06-06:** **Tutor-quiz UI uplift — Claude Design Option A** (merged to `main`): a visual redesign of the quiz **list** (`/tutor/quizzes`) + **editor** (`/tutor/quiz/[id]`) from Sam's CD "Quiz UI Uplift" handoff (List A card grid + Editor A workbench), concept-not-source, all app-layer (no migration). **List:** kind-edged elevated cards (kind tag · status dot-pill · footer tray w/ icon meta + used chip), a page eyebrow + 4-cell **stat strip** + a **toolbar** (search · status segments · Kind · Sort, client-side over `QuizListRow[]`), and the pencil replaced by a full-lifecycle **⋯ menu** (Edit · Publish/Unpublish · Archive/Restore · Delete) composing the existing actions/dialogs (shared `LeavePublishedWarning` extracted; fixed a stacking-context bug trapping the error toast under the topbar). **Editor:** kind tag + **stat chips** header, separated zone headers + "N programmes" badge, item **drag grip** (visual) + **difficulty dots** + SVG icon buttons, teal picker selection. **Picker hover-peek:** hover a clamped stem → body-portaled popover with the full stem + classification, reusing the bank's generic `HoverPeek` (`getPickerQuestions` fetches the extra columns). New `quiz-icons.tsx` + `quiz-stat-strip.tsx` + `quiz-card-menu.tsx` + `leave-published-warning.tsx`; uplift CSS appended to `quiz.css`. 4 commits `7bfc6dd` · `ac91eb5` · `ea01c63` · `07bd381`. Deferred: grid/table toggle + real drag-and-drop.](sessions/2026-06.md)
- [2026-06 — **2026-06-06:** **Tutor-quiz creation flow — lifecycle hardening + rich picker** (merged to `main`): an MVP-sweep pass over `lib/tutor-quiz/` (list · editor · lifecycle) after Slices 1–6, all app-layer (no migration). **Publish gate** (≥1 question, on publish + on removing the last question); **quick-edit pencil** on cards (opens the shared meta modal); **delete a quiz** (block-don't-cascade — blocked while linked to any activity, else type-to-confirm with a "results are kept" line; attempts survive via `quiz_id ON DELETE SET NULL`, standalone memberships cascade); **header Publish/Unpublish/Archive/Restore** controls replacing the buried Status dropdown, with an in-use warning when leaving Published (`setQuizStatusAction` + `quizUsageAction`); **kind-switch block** (a linked Mock activity must keep a Mock quiz — shared `ActivityBlockedDialog`); **"Needs questions"** cue on 0-question Draft cards; and a **rich question-picker filter** — the editor "Add questions" filter rebuilt as a live-apply faceted toolbar (8 multi-select facets + scoped multi-field search + chips), a tailored-not-shared copy of the bank list pattern (`quiz-picker-query.ts`), hard-scoped to published + standalone. 5 commits `c1ab282` · `f6ca1d9` · `f66880b` · `666c014` · +rich-picker. **Next:** a CD visual pass over the quiz list + editor.](sessions/2026-06.md)
- [2026-06 — **2026-06-06:** **Bank surfaces — the Claude Design redesign** (merged to `main`): all three bank **list** surfaces rebuilt from a CD handoff, concept-not-source (visual system only; our server-side filtering + full filter set + real data kept; CD's demo chrome not built). New `styles/bank-list.css` (`bl-*`, loaded last) + `lib/bank/list-ui.tsx` primitives; the shared `AuthorshipCell` became a **facepile**. **Trends + Cases** got an overview/health **band**, a compact toolbar (+New in-toolbar), a new **Health** column + Attached cell (Cases: chart-tab chips + "Hidden" tag; Difficulty dropped). **Question Bank Hybrid**: `BankBand` (4-segment composition w/ **Note** + clickable stat-card filters), coloured **type pills**/**difficulty chips** + **Updated** column, the grid → a compact **`BankToolbar`** (scope picker inside the search, right-docked **Filters popover** w/ all 11 facets incl. **Note-born**), **sortable headers** + **Group** toggle. **Pagination**: bank **server-side** (50 + Load more; sort/group load the full set ≤500 so the client sorts incl. Difficulty rank — ⚠ follow-up: move sort server-side before >500 questions), Trends/Cases **display pagination** (Show more). Plus a screenshot-driven polish pass (table tightening so Actions fits, stacked Attached/Health, CD page header + cross-link actions on every surface). `5c37d53` · `e4a65c3` · `990fae2` · +pagination/docs.](sessions/2026-06.md)
- [2026-06 — **2026-06-05:** **bank list pages — MVP sweep** (app-layer, no migration; merged to `main`): the `/bank/all` **filter bar rebuilt** (live-apply · wide multi-select facets · scoped multi-field search · active chips · shared `lib/bank/bank-list-query.ts`); **wrapper lists** got filters + **content search** (title/scenario/charts) + a **"Needs attention"** health filter via new `Cases/TrendsListClient`; a reusable **hover-peek** (full content, viewport-aware, scrolls) across all three lists; the questions list became a **two-row card** with a full-width strip of origin badges + **classification tags**, "Max"→"Marks", a 2-line stem clamp in a 50% column, and a slimmed Authors cell; plus the **"Note · {title}" origin badge** (surface-aware) closing a deferred library follow-on.](sessions/2026-06.md)
- [2026-06 — **2026-06-05:** authorship/audit-log **Step 2 — the readout**: "Created by / Last edited by" + a body-portaled history drawer (🕑 clock) across every bank surface — a stacked **Authors** column on all 6 lists (case/trend × admin/tutor + both `/bank/all`), the case + trend **wrapper topbars**, and a facts line atop **all 9 question editors** (host-agnostic: standalone pop-up AND embedded child editor; drawer floats over the modal). New product-wide **entity-generic `lib/audit/` module** (realm + entity_type keyed, with a per-entity-type access-gate registry → a new surface = one trigger line + one gate line). Wrapper-only on lists (a case's questions never roll up); not retroactive (pre-tracking rows show "—"). Plus a pre-existing **id auto-numbering bugfix** (`nextItemId`/`nextCaseId` collided on non-numeric/seed ids). 5 commits (`9224ea7` `6707eb1` `b072df1` `fb20b7b` + docs); **merged to `main`**.](sessions/2026-06.md)
- [2026-06 — **2026-06-05:** bank publish-integrity gates (case Publish toggle checks its 6 questions; trend question checks its dataset — mirror images) + trend builder-visible no-op explainer + all-draft Validate warning + wrapper-list published/draft pills + **authorship/audit-log foundation** (two realm-split append-only logs + shared trigger, `changed_by` + point-in-time `changed_by_name`, Step 1 applied to dev — Steps 2/3 pending; see `bank-audit-log.md`). All on the session branch, not yet merged. Earlier — Top-down MVP sweep begins: Tutor **Home** dashboard at `/tutor` (greeting + KPI cards + needs-attention + this-week sessions + programmes + workspace; new `NavItem.exact`; MERGED `bab82f0`) + **Programmes** list/modals uplift (search/filter/sort, richer cards w/ completion meter + price, CD modal restyle w/ segmented + switches, modals portaled to body to escape the card stacking-context trap). Earlier: Cohort Analytics (tutor dashboard — completion + quiz performance + per-question miss-rate, Slices 1/2/2b, on branch) + Node 20→24 CI bump (released) + library programme-integration arc RELEASED to prod (PR #24). Earlier: Tutor Library → programme integration completes: Shelf as a curriculum activity (11.12 a/b/c — atomic SHELF activity + attachment, tutor authoring with Hide/Unhide + mixed-visibility "Make visible here", student grouped render + derived rollup + TOC popup, and the "your tutor updated this shelf" drift hint via nclex_library_shelf_seen). The library is now fully integrated as both Note + Shelf activities; only 11.11c + 11.17 remain.](sessions/2026-06.md)
- [2026-05 — Programme build: foundation → curriculum → tutor-quiz → progress engine → enrolment → payments (incl. 7d installments lifecycle + System Config + 7e price_minor retirement + Slice 8 enquiries + CD-driven UI rebuild) + Tutor Library Slices 11.1 → 11.4 + post-11.4 polish + 3-slice design lock + shared NoteLensRow bundle + Library Overview + Views + sidebar polish + Slice 2.9 chrome polish (locked shell + railed sidebars + user bar) + Library 11.5 Tiptap editor scaffold (a + b: foundation + slash menu + drag handle + CD visual + rail collapse) + 11.5 follow-on polish (controlled-menu + buttons + hover/focus tint + SVG icons + toolbar expansion + swatch popovers + block tray + inter-block gaps + popover-position hook) + Slice 11.6a Image block (Supabase Storage pipeline + auto-resize + NodeView + image-persistence Server-Action serialization fix + upload-aware autosave) + Slice 11.6b PDF + Video blocks (private PDF bucket + signed URLs + video host-classify embed/link/reject + universal link-card fallback + shared upload-event) + Slice 11.6c Table block (extension-table v3 + floating contextual toolbar + 6 colour themes + TableView-nodeView data-color fix + libTableFigure note recovery) + worktree/branch cleanup (50 worktrees + 51 merged branches pruned) + Library 11.7/11.8/11.9 — the NCLEX-domain "nursing-shaped" block trio (Callout tab-chip + Drug card + Lab values grid, each CD-iterated) + Library 11.10 Publish flow/visibility/alt-text preflight (MERGED) + build-order re-sequence (programme integration last) + 11.15 Embedded-questions tutor authoring half (block + pick/create-inline + admin-config-driven caps, grandfather-safe) + 11.16a search (recursive indexer-bug fix + prefix/live + per-field scope) + tag-input discoverability fix + 11.16b clickable Tags lens + Manage-tags panel (rename/delete/merge) + 11.16c custom views (filter builder + nclex_tutor_library_views persistence + saved-view pane + sidebar scroll-cap pass + folder/shelf pane Edit/Delete) — **whole 11.16 arc MERGED to main** + the student-reading side of the library: 11.14a/b student front door (programme + cohort routes) + 11.13a note read view (custom block renderer, contents rail, bookmark/done, breadcrumb + clickable chips, named shelf chips) + 11.13b embedded-questions player (append-only attempt-history table, play_id sittings, secure load/grade, intro card + past-sittings review) — **all MERGED to main** — then (later, on session branch, NOT merged) **11.14c student Study Home + 11.11a/b Library Note as a curriculum activity (Option C) + the cohort-checklist snapshot→live three-state redesign** (77 sessions)](sessions/2026-05.md)
- [2026-04 — Bank authoring (all 9 item types + wrappers), nav scaffolds, auth, repo + DB split off gamma (44 sessions)](sessions/2026-04.md)
